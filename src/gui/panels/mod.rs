// This is not very optimal, that you would have to write a new panel for each chip/gate
// However, it saves time than creating a generic  version.
// TODO: Add generic way to add gates in the panels.

pub mod adder;
pub mod alu;
pub mod not;
